{
  "functions": { "api/chat.js": { "maxDuration": 30 } }
}
```

**Step 5** — Click **Commit changes**

Once that's done your repo will have:
```
thrive28-proxy/
├── vercel.json        ← adding this now
└── api/
    └── chat.js        ← already done ✅
